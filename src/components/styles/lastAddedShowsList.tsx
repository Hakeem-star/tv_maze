import { css } from "@emotion/react";

export const lastAddedShowsListStyle = css`
  width: 100%;
  display: grid;
  grid-template-columns: repeat(auto-fill, 140px);
  grid-gap: 20px;
  justify-content: start;
  margin-top: 40px;
`;
